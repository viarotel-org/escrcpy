interface:
  display_name: "Escrcpy"
  short_description: "Repo-specific patterns and workflows for escrcpy"
  default_prompt: "Use the escrcpy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true