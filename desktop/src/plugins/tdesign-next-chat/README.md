请基于 **@tdesign-vue-next/chat v0.4.6 ** 对现有 **/copilot 模块中的 chat 相关组件**进行系统性重构，目标是提升组件结构清晰度、可维护性与后续扩展能力。

#### 技术约束与上下文说明

1. **组件来源**

   * 当前项目已完成 `src/plugins/tdesign-next-chat/index.js` 的全局引入；
   * 在 `/copilot` 模块中可直接使用 **TDesignChat 组件库**提供的相关组件，无需再次注册或封装。
2. **文档来源说明（重要）**

   * 由于 **@tdesign-vue-next/chat** 版本较新，官方公开文档尚未完全同步；
   * 请 **以 `src/plugins/tdesign-next-chat/docs/**/*.md` 中的文档内容为唯一权威依据**，严格按照其中的 API、Props、Slots、Events 使用组件；
   * 如发现新旧版本 API 存在差异，应优先遵循当前项目中 docs 的实现方式。

  3. **补充说明**
      * 新增复制/重试能力(ChatAction)
      * 暂时禁用上传能力（图片/附件上传），相关回调可留空实现；
      * 由于 `t-chat-reasoning` **不支持通过插槽自定义主要内容**，。
        * 需要将「步骤解析器」及「任务状态（TaskStatus）」相关展示逻辑：
        * 放置在与 `t-chat-reasoning` **同级**
        * 具体实现位置为：`t-chat-message` 的 `#content` 插槽中
        * 且 **渲染顺序在 `t-chat-reasoning` 之前**，以保证思考态与任务状态的语义与展示正确。

#### 重构目标

* 保持现有业务功能与交互逻辑不变；
* 优化组件层级结构与命名语义；
* 提高代码可读性、可维护性及后续扩展能力（如多消息类型、多角色、工具消息等）。


请在实现过程中，优先遵循当前项目的编码风格与已有设计规范。